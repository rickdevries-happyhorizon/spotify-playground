# Storage backend: mysql (default) or txt (single text file, no MySQL needed)
STORAGE_BACKEND=mysql

# Text-file storage (only when STORAGE_BACKEND=txt)
# STORAGE_FILE=data/store.txt

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=spotify_playground
